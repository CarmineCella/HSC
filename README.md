# HSC

This experimental code implements hierarchical spectral clustering of scattering coefficients using MATLAB. 


Current dependencies are:

[ScatNetLight](https://github.com/edouardoyallon/ScatNetLight/releases) by E. Oyallon

The main file invokes the computation function (called decompose_path) which takes several parameters and gives back the results.
